title = "DailyPlanner gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Documentation and explicit placeholders"
regexTarget = "secret"
regexes = ['''^your_[a-z_]+$''', '''^synthetic[-_a-zA-Z0-9]*$''']
